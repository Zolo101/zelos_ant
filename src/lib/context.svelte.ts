// NOTE: Do NOT use Svelte Contexts, they are impossible to use with variables that are set in onMount.

// import type { WorkspaceSvg } from "blockly";
// import { createContext } from "svelte";
// import type Renderer from "./render/webgl2.svelte";

// maybe game & gamestate would be useful for this?
// export const [getWorkspace, setWorkspace] = createContext<WorkspaceSvg>();
// export const [getRenderer, setRenderer] = createContext<Renderer>();
